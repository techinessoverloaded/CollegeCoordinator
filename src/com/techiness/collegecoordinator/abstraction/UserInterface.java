package com.techiness.collegecoordinator.abstraction;

/**
 * An Interface used for representing the common behaviour of all UI Classes.
 */
public interface UserInterface
{
    /**
     * This method is used to display the UI and interact with the User, while performing the necessary Actions as per User Requirements.
     */
    void displayUIAndExecuteActions();
}
